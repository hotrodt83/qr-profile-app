# Required for Vercel (add these in Project Settings → Environment Variables):
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
#
# Supabase (create project at https://supabase.com)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: for private avatars bucket. When set, public profile page serves avatars via signed URLs (1h) so guests can see them without making the bucket public. If unset, the avatars bucket must be Public (Storage → Buckets → avatars → Public = ON).
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Canonical site URL for QR codes and share links (required for production).
# Dev: http://localhost:3001  |  Prod: your Vercel URL (e.g. https://smartqr.vercel.app)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Optional override (legacy): NEXT_PUBLIC_APP_URL is used if NEXT_PUBLIC_SITE_URL is not set.
# NEXT_PUBLIC_APP_URL=http://localhost:3001

# For scripts/test-save-profile.mjs (profile save test)
# TEST_EMAIL=your@email.com
# TEST_PASSWORD=your-password
